<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <title>Forgot Password - xrepu</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="auth-shell">
    <div class="login-box">
      <span class="brand-mark">xrepu</span>
      <% if (sent) { %>
        <p class="success-msg">If an account exists for that email, we've sent a password reset link. Check your inbox (and spam folder).</p>
        <p class="muted small"><a href="/login">Back to log in</a></p>
      <% } else { %>
        <p>Enter your email and we'll send you a reset link</p>
        <% if (error) { %>
          <p class="error"><%= error %></p>
        <% } %>
        <form method="POST" action="/forgot-password">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <input type="email" name="email" placeholder="Email address" required autofocus />
          <button type="submit">Send Reset Link</button>
        </form>
        <p class="muted small"><a href="/login">Back to log in</a></p>
      <% } %>
    </div>
  </div>
</body>
</html>
